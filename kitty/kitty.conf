# ╔══════════════════════════════════════════════════════════════╗
# ║  Kitty Terminal — kitty.conf                                ║
# ║  Fedora 43 · Wayland · RTX GPU · Dracula Pro                ║
# ║  Kaydet: ~/.config/kitty/kitty.conf                         ║
# ╚══════════════════════════════════════════════════════════════╝

#──────────────────────────────────────────
# FONT AYARLARI
#──────────────────────────────────────────

font_family      JetBrainsMono Nerd Font
bold_font        JetBrainsMono Nerd Font Bold
italic_font      JetBrainsMono Nerd Font Italic
bold_italic_font JetBrainsMono Nerd Font Bold Italic
font_size        13.0

# Ligature desteği
disable_ligatures never
font_features JetBrainsMonoNF-Regular +cv02 +cv03 +cv04 +ss01

# [DÜZELTİLDİ] adjust_line_height/adjust_column_width/adjust_baseline
# deprecate edildi → modify_font kullanılmalı
modify_font cell_height -1px

#──────────────────────────────────────────
# PERFORMANS (RTX GPU)
#──────────────────────────────────────────

repaint_delay    10
input_delay      3
sync_to_monitor  yes

#──────────────────────────────────────────
# TERMINAL ÖZELLİKLERİ
#──────────────────────────────────────────

term               xterm-kitty
shell              /usr/bin/fish
shell_integration  enabled
allow_hyperlinks   yes
allow_remote_control socket-only
listen_on          unix:/tmp/kitty-{kitty_pid}

#──────────────────────────────────────────
# RENK PALETİ — Dracula Pro
#──────────────────────────────────────────

background           #22212c
foreground           #f8f8f2
cursor               #f8f8f2
cursor_text_color    background
selection_foreground none
selection_background #454158

# Black
color0  #22212c
color8  #6272a4
# Red
color1  #ff9580
color9  #ffaa99
# Green
color2  #8aff80
color10 #a2ff99
# Yellow
color3  #ffff80
color11 #ffff99
# Blue
color4  #9580ff
color12 #aa99ff
# Magenta
color5  #ff80bf
color13 #ff99cc
# Cyan
color6  #80ffea
color14 #99ffee
# White
color7  #f8f8f2
color15 #ffffff

# URL
url_color  #80ffea
url_style  curly
detect_urls yes
open_url_with default

#──────────────────────────────────────────
# PENCERE AYARLARI
#──────────────────────────────────────────

remember_window_size  yes
initial_window_width  1200
initial_window_height 700

window_border_width    1px
window_margin_width    2
window_padding_width   2
placement_strategy     center

active_border_color    #ff80bf
inactive_border_color  #6272a4

# Şeffaflık + blur
background_opacity          1
dynamic_background_opacity  yes
background_blur             15

# Pencere dekorasyonu
hide_window_decorations no
confirm_os_window_close 0
resize_debounce_time    0.1
resize_in_steps         yes

# Wayland
linux_display_server    auto
wayland_titlebar_color  system

#──────────────────────────────────────────
# SEKME AYARLARI
#──────────────────────────────────────────

tab_bar_edge       bottom
tab_bar_style      powerline
tab_powerline_style round
tab_bar_align      left
tab_bar_margin_width 2
tab_bar_margin_height 5 2

# Tab renkleri
tab_bar_background      #22212c
active_tab_background   #454158
active_tab_foreground   #f8f8f2
inactive_tab_background #22212c
inactive_tab_foreground #6272a4

# Tab başlık şablonu
tab_title_max_length 25
tab_title_template          " {fmt.fg.red}{bell_symbol}{activity_symbol}{fmt.fg.tab}{index}:{title[:15]}{'…' if len(title) > 15 else ''} "
active_tab_title_template   " {fmt.fg._ff80bf}{bell_symbol}{activity_symbol}{fmt.fg.tab}{index}:{title[:15]}{'…' if len(title) > 15 else ''} "

#──────────────────────────────────────────
# İMLEÇ AYARLARI
#──────────────────────────────────────────

cursor_shape               block
cursor_beam_thickness      2.0
cursor_underline_thickness 2.0
cursor_blink_interval      0.6
cursor_stop_blinking_after 10.0

#──────────────────────────────────────────
# LAYOUT
#──────────────────────────────────────────

enabled_layouts tall:bias=50;full_size=1;mirrored=false,fat:bias=50;full_size=1;mirrored=false,splits,stack
inactive_text_alpha 0.7

#──────────────────────────────────────────
# SCROLLBACK
#──────────────────────────────────────────

scrollback_lines                200000
scrollback_pager_history_size   100
scrollback_fill_enlarged_window no
wheel_scroll_multiplier         5.0
wheel_scroll_min_lines          1
touch_scroll_multiplier         1.0

#──────────────────────────────────────────
# MOUSE AYARLARI
#──────────────────────────────────────────

mouse_hide_wait       2.0
url_prefixes          file ftp ftps gemini git gopher http https irc ircs kitty mailto news sftp ssh
show_hyperlink_targets yes
underline_hyperlinks  hover

mouse_map left click ungrabbed mouse_handle_click selection link prompt
mouse_map left press ungrabbed mouse_selection normal
mouse_map left doublepress ungrabbed mouse_selection word

#──────────────────────────────────────────
# BELL / BİLDİRİM
#──────────────────────────────────────────

enable_audio_bell       no
visual_bell_duration    0.0
window_alert_on_bell    yes
bell_on_tab             "🔔 "
notify_on_cmd_finish    unfocused 5.0

#──────────────────────────────────────────
# CLIPBOARD / PANO
#──────────────────────────────────────────

copy_on_select          a1
paste_actions           quote-urls-at-prompt
strip_trailing_spaces   never

#──────────────────────────────────────────
# GELİŞMİŞ AYARLAR
#──────────────────────────────────────────

allow_cloning          ask
clone_source_strategies venv,conda,env_var,path
update_check_interval  0
startup_session        none
close_on_child_death   no
kitten_alias           hints hints --hints-offset=0

#──────────────────────────────────────────
# KLAVYE KISAYOLLARI
#──────────────────────────────────────────

clear_all_shortcuts yes

# ── Pencere Yönetimi ────────────────────
enabled_layouts splits,tall,fat,stack
map ctrl+n      launch --location=vsplit --cwd=current
map ctrl+h      launch --location=hsplit --cwd=current
map ctrl+w      close_window
# map ctrl+right  next_window
# map ctrl+left   previous_window
map ctrl+f      move_window_forward
map ctrl+b      move_window_backward
map ctrl+alt+minus      new_window


# ── Pencere Odak (Vi-like: Alt + h/j/k/l) ──
map alt+a               neighboring_window left
map alt+s               neighboring_window down
map alt+w               neighboring_window up
map alt+d               neighboring_window right

# ── Pencere Boyutlandırma (Ctrl+Alt + yön) ──
map ctrl+right       resize_window narrower
map ctrl+left       resize_window wider
map ctrl+down       resize_window taller
map ctrl+up         resize_window shorter 3

# ── Layout ──────────────────────────────
# [DÜZELTİLDİ] ctrl+shift+l/h çakışıyordu → ctrl+shift+space
map ctrl+shift+space    next_layout
map ctrl+alt+t          goto_layout tall
map ctrl+alt+s          goto_layout stack
map ctrl+alt+p          goto_layout splits

# ── Sekme (Tab) Yönetimi ───────────────
map ctrl+shift+t        new_tab
map ctrl+shift+q        close_tab
map ctrl+shift+.        move_tab_forward
map ctrl+shift+,        move_tab_backward
map ctrl+shift+alt+t    set_tab_title

# ── Sekme Navigasyonu ──────────────────
map ctrl+1              goto_tab 1
map ctrl+2              goto_tab 2
map ctrl+3              goto_tab 3
map ctrl+4              goto_tab 4
map ctrl+5              goto_tab 5
map ctrl+6              goto_tab 6
map ctrl+7              goto_tab 7
map ctrl+8              goto_tab 8
map ctrl+9              goto_tab 9

# ── Font Boyutu ────────────────────────
map ctrl+shift+equal    change_font_size all +2.0
map ctrl+shift+plus     change_font_size all +2.0
# [DÜZELTİLDİ] minus çakışması → backspace ile sıfırlama
map ctrl+shift+minus    change_font_size all -2.0
map ctrl+shift+0        change_font_size all 0

# ── Şeffaflık Kontrolü ────────────────
map ctrl+shift+a>m      set_background_opacity +0.1
map ctrl+shift+a>l      set_background_opacity -0.1
map ctrl+shift+a>1      set_background_opacity 1
map ctrl+shift+a>d      set_background_opacity default

# ── Kopyala / Yapıştır ────────────────
map ctrl+shift+c        copy_to_clipboard
map ctrl+shift+v        paste_from_clipboard
map ctrl+shift+s        paste_from_selection

# ── Kaydırma (Scrolling) ──────────────
# [DÜZELTİLDİ] ctrl+shift+j/k/up/down çakışıyordu
# → Page Up/Down ve Home/End ile kaydırma
map ctrl+shift+page_up   scroll_page_up
map ctrl+shift+page_down scroll_page_down
map ctrl+shift+home      scroll_home
map ctrl+shift+end       scroll_end
map shift+up             scroll_line_up
map shift+down           scroll_line_down

# ── Çeşitli ───────────────────────────
map ctrl+shift+f11      toggle_fullscreen
map ctrl+shift+f10      toggle_maximized
map ctrl+shift+u        kitten unicode_input
map ctrl+shift+f2       edit_config_file
map ctrl+shift+escape   kitty_shell window
map ctrl+shift+f5       load_config_file
